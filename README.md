
# 🧩 API Symfony - Gestion des Participants

Cette API permet de gérer les entités **Participant** (CRUD : Create, Read, Update, Delete) à travers des endpoints RESTful.  
Elle est développée en **Symfony** avec **Doctrine ORM**.

---

## 🗂️ Table des routes pour participant

| Méthode | URL                    | Action                       |
|--------:|------------------------|------------------------------|
| `GET`   | `/participant/{id}`    | Récupérer un participant     |
| `POST`  | `/participant`         | Créer un nouveau participant |
| `PUT`   | `/{id}`                | Modifier un participant      |
| `DELETE`| `/participant/{id}`    | Supprimer un participant     |

---
